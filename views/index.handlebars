</ul>

<h2>List of Burgers To Eat: </h2>

<ul>
  {{#each burger}}
  {{#unless consumed}}
  {{> cats/burger-block consumed=true}}
  {{/unless}}
  {{/each}}
</ul>

<h2>Create Your Burger!</h2>
<form id="create-form" action="/" method="POST">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>
  <button type="submit" class="btn-primary">Submit Burger Order</button>


</form>